APP_NAME=ClientKosmos
NODE_ENV=development

APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
#DB_HOST=127.0.0.1
#DB_PORT=3306
#DB_DATABASE=clientkosmos
#DB_USERNAME=root
#DB_PASSWORD=
# DB_SSL_CA=  # Solo si tu servidor MySQL requiere SSL

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Broadcasting deshabilitado por defecto. Ver docs/broadcasting-disabled.md para
# entender por qué y los pasos exactos para reactivarlo (Reverb o Ably).
BROADCAST_CONNECTION=null
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# Variables de Reverb (websockets self-hosted). Vacías a propósito mientras
# BROADCAST_CONNECTION=null; rellenarlas no activa nada por sí solo.
REVERB_APP_ID=
REVERB_APP_KEY=
REVERB_APP_SECRET=
REVERB_HOST="localhost"
REVERB_PORT=8080
REVERB_SCHEME=http

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ─── Correo ────────────────────────────────────────────────────────────────────
# Local (Docker): MAIL_MAILER=smtp con MAIL_HOST=mailpit (inyectado por
#   docker-compose). Bandeja en http://localhost:8025.
# Local (sin Docker): MAIL_MAILER=log escribe los emails a storage/logs.
# Producción (Railway): se usa Brevo SMTP (free tier 300 emails/día, sin
#   necesidad de dominio propio — basta con verificar un email remitente).
#   Las verificaciones de email y resets de contraseña se encolan en
#   QueuedVerifyEmail / QueuedResetPassword para no bloquear la request.
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=tuemail@gmail.com
MAIL_PASSWORD=tu_contraseña_de_aplicación
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=tuemail@gmail.com
MAIL_FROM_NAME="${APP_NAME}"

# ─── Brevo (producción, free) ──────────────────────────────────────────────────
# Configuración actual en Railway. Para replicar:
#   1. Crear cuenta en https://www.brevo.com (no pide tarjeta).
#   2. Verificar un sender email (Settings → Senders, Domains & Dedicated IPs).
#   3. Generar credenciales SMTP en SMTP & API → SMTP.
#   4. Sobreescribir en Railway:
#        MAIL_MAILER=smtp
#        MAIL_HOST=smtp-relay.brevo.com
#        MAIL_PORT=587
#        MAIL_USERNAME=<el login que muestra Brevo, p. ej. 8abc12001@smtp-brevo.com>
#        MAIL_PASSWORD=<master password SMTP>
#        MAIL_ENCRYPTION=tls
#        MAIL_FROM_ADDRESS=<email verificado en Brevo>
#        MAIL_FROM_NAME=ClientKosmos

# ─── Resend (alternativa con dominio propio) ───────────────────────────────────
# resend/resend-laravel instalado. Si compras dominio y lo verificas en
# https://resend.com/domains, conmuta a Resend:
#   MAIL_MAILER=resend
#   RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
#   MAIL_FROM_ADDRESS=hola@tu-dominio.com
RESEND_API_KEY=

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

GROQ_API_KEY=
GROQ_BASE_URL=https://api.groq.com/openai/v1
GROQ_MODEL=llama-3.3-70b-versatile
# GROQ_CA_BUNDLE=  # Solo en Windows si cURL no tiene CA bundle

# Stripe (test mode only) — ver ADR-0019
STRIPE_KEY=
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=

# Google Calendar OAuth — ver ADR-0023
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI="${APP_URL}/settings/google/callback"

VITE_APP_NAME="${APP_NAME}"
